# payment-service, local development. Owns `paymentdb`.

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5436/paymentdb}
    username: paymentdb
    password: paymentdb
  flyway:
    locations: classpath:db/migration

app:
  order-service:
    url: ${ORDER_SERVICE_URL:http://localhost:8083}
