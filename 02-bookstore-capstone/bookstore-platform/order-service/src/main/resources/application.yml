spring:
  application:
    name: order-service
  profiles:
    default: dev
  jpa:
    open-in-view: false

server:
  port: 8083
  error:
    include-stacktrace: never

app:
  jwt:
    expiration-minutes: 60
    issuer: bookstore
